// Server removed: job proxy server was intentionally deleted.
// The project now calls job providers directly from the client using Vite env vars
// (see `README.md` and `src/components/JobSearch/JobSearch.jsx`).

export default {};
